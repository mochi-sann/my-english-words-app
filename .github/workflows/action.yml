name: stories-Github-page
on:
  push:
    branches:
      - main
      - releases/*

jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: node -v
      # - run: npm install
      # - run: npm run storybook:gh -ci
